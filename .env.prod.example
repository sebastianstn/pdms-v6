# ═══════════════════════════════════════════════════════════════
# PDMS Home-Spital — PRODUKTIONS-Umgebungsvariablen (Raspberry Pi)
# ═══════════════════════════════════════════════════════════════
# Verwendung:
#   cp .env.prod.example .env
#   # Danach alle Platzhalter (<...>) ersetzen
#
# WICHTIG:
# - Niemals diese Datei mit echten Secrets ins Git pushen
# - Verwende starke Passwörter (mind. 20 Zeichen)
# - In Produktion niemals Dev-Bypass aktivieren
# ═══════════════════════════════════════════════════════════════

# ─── Domain / Umgebung ──────────────────────────────────────────
PDMS_DOMAIN=<deine-domain.tld>
ENVIRONMENT=production
LOG_LEVEL=WARNING

# ─── PostgreSQL (Pflicht) ───────────────────────────────────────
POSTGRES_PASSWORD=<starkes-postgres-passwort>

# ─── Keycloak (Pflicht) ─────────────────────────────────────────
KC_ADMIN_USER=admin
KC_ADMIN_PASSWORD=<starkes-keycloak-admin-passwort>
KC_API_SECRET=<starkes-keycloak-api-client-secret>

# ─── RabbitMQ (Pflicht) ─────────────────────────────────────────
RABBITMQ_PASSWORD=<starkes-rabbitmq-passwort>

# ─── Valkey (dringend empfohlen) ────────────────────────────────
VALKEY_PASSWORD=<starkes-valkey-passwort>

# ─── Frontend / Auth ───────────────────────────────────────────
# In Produktion KEIN Auth-Bypass aktivieren.
# NEXT_PUBLIC_DEV_AUTH_BYPASS=false

# ─── AI Orchestrator (optional) ─────────────────────────────────
# GEMINI_API_KEY=
# CLAUDE_API_KEY=
# OLLAMA_BASE_URL=http://host.docker.internal:11434
