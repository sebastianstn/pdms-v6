# ═══════════════════════════════════════════════════════════════
# PDMS Home-Spital — Umgebungsvariablen
# ═══════════════════════════════════════════════════════════════
# Kopiere diese Datei nach .env und passe ALLE Werte an:
#   cp .env.example .env
#
# ⚠️  SICHERHEITSHINWEIS:
# - Niemals .env ins Git einchecken (.gitignore prüfen!)
# - Alle Passwörter in Produktion durch SICHERE Werte ersetzen
#   (min. 16 Zeichen, Sonderzeichen)
# - NEXT_PUBLIC_DEV_AUTH_BYPASS darf in Produktion NIEMALS "true" sein
# ═══════════════════════════════════════════════════════════════

# ─── PostgreSQL (PFLICHT) ─────────────────
# Produktion: Sicheres Passwort verwenden (min. 16 Zeichen)!
POSTGRES_PASSWORD=pdms_secret_2026

# ─── Keycloak Admin (PFLICHT) ─────────────
# Produktion: Kein "admin" — sicheres Passwort verwenden!
KC_ADMIN_PASSWORD=admin

# ─── Keycloak API Secret (PFLICHT) ────────
KC_API_SECRET=dev-secret

# ─── RabbitMQ (PFLICHT) ──────────────────
# Produktion: Sicheres Passwort verwenden!
RABBITMQ_PASSWORD=pdms_rabbit_2026

# ─── App Settings ─────────────────────────
LOG_LEVEL=DEBUG
ENVIRONMENT=development

# ─── Auth-Bypass (NUR Development!) ───────
# ⚠️  WARNUNG: Umgeht die Keycloak-Authentifizierung komplett!
# In Produktion: Diese Zeile LÖSCHEN oder auf "false" setzen.
# NEXT_PUBLIC_DEV_AUTH_BYPASS=true

# ─── AI Orchestrator (optional) ──────────
# GEMINI_API_KEY=
# CLAUDE_API_KEY=
# OLLAMA_BASE_URL=http://host.docker.internal:11434
