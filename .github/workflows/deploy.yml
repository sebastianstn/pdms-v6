name: Deploy — Build + Docker Push

on:
  push:
    tags: ["v*"]

jobs:
  build:
    name: "Build & Push Docker Images"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build API image
        run: docker build -t pdms-api:${{ github.ref_name }} apps/api/
      - name: Build Web image
        run: |
          echo "TODO: production Dockerfile for Next.js"
      # TODO: Push to container registry
